import pandas as pd\nimport numpy as np\n\n# Load the dataset\ndef load_data(filepath):\n    return pd.read_csv(filepath)\n\n# Handle missing values\ndef handle_missing_values(df):\n    # Fill missing numerical values with the mean\n    num_cols = df.select_dtypes(include=[np.number]).columns\n    df[num_cols] = df[num_cols].fillna(df[num_cols].mean())\n\n    # Fill missing categorical values with the mode\n    cat_cols = df.select_dtypes(include=[object]).columns\n    df[cat_cols] = df[cat_cols].fillna(df[cat_cols].mode().iloc[0])\n\n    return df\n\n# Remove duplicates\ndef remove_duplicates(df):\n    return df.drop_duplicates()\n\n# Detect and remove outliers\ndef remove_outliers(df):\n    for col in df.select_dtypes(include=[np.number]).columns:\n        Q1 = df[col].quantile(0.25)\n        Q3 = df[col].quantile(0.75)\n        IQR = Q3 - Q1\n        df = df[~((df[col] < (Q1 - 1.5 * IQR)) | (df[col] > (Q3 + 1.5 * IQR)))]\n    return df\n\n# Standardize data types\ndef standardize_data_types(df):\n    # Example: Convert price column to float\n    if 'price' in df.columns:\n        df['price'] = df['price'].astype(float)\n    return df\n\n# Validate data quality\ndef validate_data_quality(df):\n    # Check for negative prices\n    if (df['price'] < 0).any():\n        raise ValueError("Data quality issue: Negative prices found.")\n    return df\n\n# Main function\ndef main():\n    df = load_data('nigeria_houses_data.csv')\n    df = handle_missing_values(df)\n    df = remove_duplicates(df)\n    df = remove_outliers(df)\n    df = standardize_data_types(df)\n    df = validate_data_quality(df)\n    \n    # Save the cleaned dataset\n    df.to_csv('nigeria_houses_data_cleaned.csv', index=False)\n\nif __name__ == "__main__":\n    main()\n